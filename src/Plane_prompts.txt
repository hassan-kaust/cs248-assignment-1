PROMPT #1:

1. create a Plane class with with a point (Vec3) and a normal (Vec3)

2. create intersect(ray) method:
    - compute denom = dot(normal, ray.d)
    - if abs(denom) < epsilon, return - 1
    - compute t = dot(normal, point - ray.o)/denom
   - return t if t > epsilon, else return -1

3. create getNormal(point) method:
   - return normal (ignore the input point, its constant)